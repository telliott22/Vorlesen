# Google Cloud Text-to-Speech API credentials
# Choose ONE of the following options:

# Option 1: API Key (simplest, if available)
GOOGLE_CLOUD_TTS_API_KEY=your_api_key_here

# Option 2: Service Account JSON (for Vercel deployment)
# Paste entire contents of service-account.json as single-line string
# GOOGLE_CLOUD_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}

# Option 3: Service Account file path (local development only)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
